import 'package:flutter/material.dart';

class AnalyticInfo {
  final String? svgSrc, title;
  final int? totalValue;
  final Color? color;

  AnalyticInfo({
    this.svgSrc,
    this.title,
    this.totalValue,
    this.color,
  });
}
